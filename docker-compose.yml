# docker-compose.yml
services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    networks:
      - app_net
    command: sh -c "python app/manage.py migrate && python app/manage.py runserver 0.0.0.0:8000"

networks:
  app_net:
    driver: bridge